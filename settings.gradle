//include ':stronglib'

def openApp = Integer.parseInt(openApp)
def isOpenAppAll = Boolean.parseBoolean(isOpenAppAll)
if (openApp == 1 || isOpenAppAll) {
    include ':app'
}
if (openApp == 2 || isOpenAppAll) {
    include ':yoojin'
}